{
  "name": "express-deployment-demo",
  "version": "1.0.0",
  "description": "A demo application to test the automatic deployment of express application on a Windows Server",
  "main": "./src/server.js",
  "scripts": {
    "start": "start nodemon ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Jimmaphy/demo-express-deployment.git"
  },
  "private": true,
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.2"
  }
}
